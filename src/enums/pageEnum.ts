export enum PageEnum {
  // Log in
  BASE_LOGIN = '/login',
  BASE_LOGIN_NAME = 'Login',
  //Redirect
  REDIRECT = '/redirect',
  REDIRECT_NAME = 'Redirect',
  // Front page
  BASE_HOME = '/dashboard',
  //Home page jump default route
  BASE_HOME_REDIRECT = '/dashboard/console',
  // Front page
  ERROR_PAGE_NAME = 'ErrorPage',
}
